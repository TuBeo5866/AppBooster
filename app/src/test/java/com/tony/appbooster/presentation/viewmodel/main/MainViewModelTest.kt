package com.tony.appbooster.presentation.viewmodel.main

// Intentionally left empty.
//
// This project currently ships with JUnit4 only and doesn't declare MockK/Turbine/JUnit5
// dependencies in `app/build.gradle.kts`. Once those are added, we can reintroduce UDF
// contract tests for MainViewModel (event -> effect/state).
